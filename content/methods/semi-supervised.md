# Unsupervised and Semi-Supervised Methods

These methods learn regularity primarily from normal video and use language-aligned representations to broaden semantic coverage. The core challenge is to benefit from pretrained knowledge without quietly importing test categories or external anomaly labels.

## Representative pattern

A visual encoder supplies clip features; text prototypes or a language model provide contextual concepts; a temporal model estimates deviation. Complete reviewed method coverage will replace this sample prose.
